---
import type { ProjectDetailViewModel } from '../lib/project-detail/types.ts';
import { getProjectWorldDefinition } from '../lib/project-worlds/world-registry.ts';
import GlobalShell from './GlobalShell.astro';
import '../styles/project-detail.css';

interface Props {
  project: ProjectDetailViewModel;
}

const { project } = Astro.props;
const world = getProjectWorldDefinition(project.category);
---

<GlobalShell
  title={project.seoTitle}
  description={project.seoDescription}
  canonical={project.canonical}
  noindex={project.noindex}
  currentPath={project.href}
  shellProfile={world.shellProfile}
  worldProfile={world.worldProfile}
  mainId="project-detail"
>
  <article
    class="project-detail"
    data-project-category={project.category}
    data-project-layout-profile={world.layoutProfile}
    data-project-theme-profile={world.themeProfile}
    data-project-motion-profile={world.motionProfile}
    aria-labelledby="project-title"
  >
    <slot />
  </article>
</GlobalShell>
