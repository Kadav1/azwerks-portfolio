---
import type { ProjectDetailViewModel } from '../../lib/project-detail/types.ts';
import ProjectWorld from '../project-worlds/ProjectWorld.astro';

interface Props {
  project: ProjectDetailViewModel;
  Content: any;
  hasNarrative: boolean;
}

const { project, Content, hasNarrative } = Astro.props;
---

<ProjectWorld project={project} Content={Content} hasNarrative={hasNarrative} />
